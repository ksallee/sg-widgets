/**
 * Deprecated. `ListSelect` is `ListPicker`: install `list-picker` and import that.
 * This module keeps an installed item resolving for one release.
 */
export {
  ListPicker as ListSelect,
  type ListOption,
  type ListPickerProps as ListSelectProps,
  type ListPickerSize as ListSelectSize,
} from '@/registry/sg/components/list-picker';
