# @sg-widgets/svelte

The shadcn-svelte registry: every widget as a registry item, built here and served from the docs
site at `/r/svelte/<name>.json`.

Install commands and what the set is: the [repository README](../../README.md). A page per widget,
with a live demo: [sg-widgets.vercel.app](https://sg-widgets.vercel.app).
