import type { PropsFile } from './_types';

/** Two functions, not a component: the page documents them in prose. */
export default {
  page: 'value-editor',
  declares: false,
} satisfies PropsFile;
