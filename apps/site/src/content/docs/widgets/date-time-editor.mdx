---
title: DateTimeEditor
description: A date_time field as a date and a time input, shown local and stored UTC.
---

import Demo from '../../../components/Demo.astro';

Edits a date_time field in the viewer's wall-clock time and emits the UTC instant the field stores.

## Install

```sh
pnpm dlx shadcn@latest add https://sg-widgets.dev/r/react/date-time-editor.json
pnpm dlx shadcn-svelte@latest add https://sg-widgets.dev/r/svelte/date-time-editor.json
```

<Demo name="date-time-editor" title="DateTimeEditor: the same instant in two zones, and an unset field" />

## Input

| typed, in `America/Los_Angeles` | emitted |
|---|---|
| `2026-03-04` and `05:06` | `"2026-03-04T13:06:00Z"` |
| `2026-03-04` and `05:06:07`, with seconds shown | `"2026-03-04T13:06:07Z"` |
| `2026-03-04` and no time | `"2026-03-04T08:00:00Z"` |
| both empty | `null` |
| a time and no date | nothing; a time needs a date |

The zone the typed time is read in is named under the control.

## Props

| prop | type | default | meaning |
|---|---|---|---|
| `value` | `string \| null` | `null` | The stored instant, UTC. Two-way in Svelte. |
| `onValueChange` | `(value: string \| null) => void` | — | Called when either input commits or a day is picked. |
| `field` | `FieldSchema \| null` | `null` | Supplies the accessible label and the required flag. |
| `timeZone` | `string` | the runtime's | IANA zone the typed wall-clock time is read in. |
| `showSeconds` | `boolean` | `false` | Seconds in the time input. |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Control height. |
| `disabled` | `boolean` | `false` | |
| `readonly` | `boolean` | `false` | |
| `invalid` | `boolean` | `false` | Forced invalid state. |
| `error` | `string \| null` | `null` | A message from the caller, shown in place of the parse error. |
| `onErrorChange` | `(error: string \| null) => void` | — | Called when the parse error appears or clears. |
| `placeholder` | `string` | `'YYYY-MM-DD'` | Placeholder of the date input. |
| `class` / `className` | `string` | — | Merged after the widget's own classes. |

## Events

`onValueChange` fires on commit and on a pick. Input that does not parse emits nothing.

## Slots

`errorMessage` receives the message and renders it.

## Keyboard

| key | does |
|---|---|
| `Enter` | Commits both inputs. |
| `Escape` | Restores the stored value, or closes the calendar. |
| `Tab` | Moves through date, calendar button and time. |
| arrows | Move between days inside the calendar, and between time segments inside the time input. |

## API behaviour

The field is stored and read as UTC `YYYY-MM-DDTHH:MM:SSZ` at second resolution. A written offset is
normalised away and a zoneless string is taken as UTC, not as site-local, so a wall-clock time has to
be converted before it is sent (field_types/date_time).

Only `null` clears the field; the empty string is a 400 (field_types/date_time).

Most timestamp fields are server-managed, and the name does not predict it. Read the schema's
`editable` flag before offering this control (field_types/date_time).
